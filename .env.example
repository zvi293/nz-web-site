# The site is fully static — no backend, no database, no API keys.
# All content lives in src/content/*.ts.

VITE_SITE_URL=https://nz-web.com

# Analytics & marketing pixels (optional — leave blank to disable).
# Trackers load only after the visitor accepts cookies.
VITE_GA_ID=
VITE_FB_PIXEL_ID=
